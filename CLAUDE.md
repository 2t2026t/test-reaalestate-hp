# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## プロジェクト概要

チャットボット付きの個人ホームページ（自己紹介ページ）を構築するプロジェクトです。  
現在は `index.html` 単一ファイルの静的サイトですが、今後チャットボット機能を追加します。

---

## 開発ルール

### 言語・コミュニケーション
- **返答は必ず日本語で行う**
- **コード内のコメントは日本語で書く**

### デザイン
- **モダンで洗練されたデザインを維持する**
- 既存のデザイントークン（CSS カスタムプロパティ）を踏襲する
- **レスポンシブ対応必須**：スマートフォン・タブレット・PCすべてで見やすいレイアウトにする

---

## アーキテクチャ

### 現在の構成

```
index.html   ← HTML・CSS・JS をすべて1ファイルにまとめた静的サイト
```

### CSS 設計

スタイルはすべて `<style>` タグ内に記述。  
カラーパレットは `:root` の CSS カスタムプロパティで一元管理しており、色変更はここだけ修正すれば全体に反映される。

```css
:root {
  --bg: #020204;        /* ページ背景（超ダーク） */
  --surface: #080810;   /* サーフェス背景 */
  --card: #0e0e18;      /* カード背景 */
  --accent: #6c63ff;    /* メインアクセント（パープル） */
  --accent2: #ff6584;   /* サブアクセント（ピンク） */
  --text: #e8e8f0;      /* メインテキスト */
  --muted: #8888aa;     /* サブテキスト */
  --border: #2a2a3a;    /* ボーダー */
}
```

### レイアウト構造

```
.container（max-width: 780px, 中央寄せ）
├── .hero          ← 名前・職業・自己紹介・CTAボタン
├── .grid          ← スキルカード3列（CSS Grid, auto-fit）
└── .status-bar    ← ステータス・拠点・経験年数
```

---

## 今後の実装予定：チャットボット

チャットボット UI を追加する際は以下の方針で実装する。

- `index.html` にチャット UI を追加（ウィジェット形式 or 専用セクション）
- デザインは既存のダークテーマ・アクセントカラーに統一する
- API 通信は `fetch` を使用し、JS は `<script>` タグ内に記述する
- チャット UI に必要な要素：送信フォーム・メッセージバブル・ローディング表示
